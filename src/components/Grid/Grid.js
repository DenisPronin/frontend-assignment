import React, { PropTypes } from 'react'

export class Grid extends React.Component {
  static propTypes = {
  };

  render () {
    return (
      <div>
        grid
      </div>
    )
  }

}

export default Grid
